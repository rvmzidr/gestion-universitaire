<div class="container">
    <div class="page-header">
        <h1>📥 Importer des étudiants</h1>
        <a href="/etudiants" class="btn btn-secondary">← Retour à la liste</a>
    </div>

    {{#if error}}
        <div class="alert alert-error">{{error}}</div>
    {{/if}}

    {{#if success}}
        <div class="alert alert-success">{{success}}</div>
    {{/if}}

    {{#if errors}}
        <div class="alert alert-warning">
            <strong>⚠️ Erreurs détectées :</strong>
            <ul style="margin-top: 10px; margin-left: 20px;">
                {{#each errors}}
                    <li>{{this}}</li>
                {{/each}}
            </ul>
        </div>
    {{/if}}

    <div class="form-container">
        <div class="info-card">
            <h3>📋 Instructions</h3>
            <p>Importez plusieurs étudiants en une seule fois à partir d'un fichier CSV.</p>
            <ul style="margin-left: 20px; margin-top: 10px; color: var(--text-secondary);">
                <li>Le fichier doit être au format CSV (séparateur : virgule)</li>
                <li>Colonnes requises : <strong>nom</strong>, <strong>prenom</strong>, <strong>email</strong>, <strong>numero_etudiant</strong></li>
                <li>Colonnes optionnelles : <strong>date_naissance</strong>, <strong>telephone</strong>, <strong>adresse</strong>, <strong>groupe</strong>, <strong>specialite</strong></li>
                <li>Les noms de groupe et spécialité doivent correspondre exactement aux valeurs existantes</li>
                <li>Format de date : YYYY-MM-DD (ex: 2000-05-15)</li>
                <li>Taille maximale : 5 MB</li>
            </ul>
            <div style="margin-top: 15px;">
                <a href="/etudiants/template" class="btn btn-info btn-sm">⬇️ Télécharger le modèle CSV</a>
            </div>
        </div>

        <div style="display: grid; grid-template-columns: 1fr 1fr; gap: 20px; margin-bottom: 20px;">
            {{#if groupes}}
            <div class="info-card" style="border-left-color: var(--secondary-color);">
                <h3>👥 Groupes disponibles</h3>
                <div style="display: flex; flex-wrap: wrap; gap: 8px; margin-top: 10px;">
                    {{#each groupes}}
                        <span class="badge badge-primary">{{this.nom}}</span>
                    {{/each}}
                </div>
            </div>
            {{/if}}

            {{#if specialites}}
            <div class="info-card" style="border-left-color: var(--accent-color);">
                <h3>🎓 Spécialités disponibles</h3>
                <div style="display: flex; flex-wrap: wrap; gap: 8px; margin-top: 10px;">
                    {{#each specialites}}
                        <span class="badge badge-warning">{{this.nom}}</span>
                    {{/each}}
                </div>
            </div>
            {{/if}}
        </div>

        <form action="/etudiants/import" method="POST" enctype="multipart/form-data">
            <div class="form-group">
                <label for="csvFile">📄 Sélectionner le fichier CSV :</label>
                <input 
                    type="file" 
                    id="csvFile" 
                    name="csvFile" 
                    accept=".csv" 
                    required
                    style="padding: 10px; border: 2px dashed var(--border-color); border-radius: var(--radius-md); background: var(--bg-secondary);"
                >
                <small style="color: var(--text-muted); display: block; margin-top: 5px;">
                    Formats acceptés : .csv (max 5 MB)
                </small>
            </div>

            <div class="form-actions">
                <button type="submit" class="btn btn-primary">📤 Importer les étudiants</button>
                <a href="/etudiants" class="btn btn-secondary">Annuler</a>
            </div>
        </form>

        <div class="info-card" style="margin-top: 30px; border-left-color: var(--info-color);">
            <h3>💡 Exemple de fichier CSV</h3>
            <pre style="background: var(--bg-secondary); padding: 15px; border-radius: var(--radius-md); overflow-x: auto; font-size: 0.85rem;">nom,prenom,email,numero_etudiant,date_naissance,telephone,adresse,groupe,specialite
Dupont,Jean,jean.dupont@etudiant.fr,E12345,2000-05-15,0123456789,123 Rue de Paris,Groupe A,Informatique
Martin,Marie,marie.martin@etudiant.fr,E12346,2001-08-20,0987654321,456 Avenue des Champs,Groupe B,Mathématiques
Bernard,Pierre,pierre.bernard@etudiant.fr,E12347,1999-12-10,0147258369,789 Boulevard Victor Hugo,Groupe A,Physique</pre>
        </div>
    </div>
</div>

<style>
    input[type="file"] {
        cursor: pointer;
    }
    
    input[type="file"]:hover {
        border-color: var(--primary-color) !important;
        background: var(--bg-primary) !important;
    }

    pre {
        white-space: pre;
        word-wrap: normal;
        margin: 0;
        font-family: 'Courier New', monospace;
        color: var(--text-primary);
    }

    @media (max-width: 768px) {
        .form-container > div[style*="grid-template-columns"] {
            grid-template-columns: 1fr !important;
        }
    }
</style>
